# Keep text files normalized in the repository and checked out as LF.
* text=auto eol=lf

# Binary assets.
*.bin binary -eol
*.icns binary -eol
*.ico binary -eol
*.jpg binary -eol
*.jpeg binary -eol
*.png binary -eol
*.webp binary -eol
